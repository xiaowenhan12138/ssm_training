<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <settings>
		<!-- mybatis控制台LOG输出 -->
        <setting name="logImpl" value="LOG4J2" />
        
        <!-- 按需加载对象属性 -->
        <!-- lazyLoadingEnabled：true使用延迟加载，false禁用延迟加载。默认为true
			aggressiveLazyLoading：true启用时，当延迟加载开启时访问对象中一个懒对象属性时，将完全加载这个对象的所有懒对象属性。false，当延迟加载时，按需加载对象属性（即访问对象中一个懒对象属性，不会加载对象中其他的懒对象属性）。默认为true -->
        <setting name="lazyLoadingEnabled" value="true"/>  
        <setting name="aggressiveLazyLoading" value="false"/>  
    </settings>
    <!-- 
    <typeAliases>
       <typeAlias type="cn.tedu.store.bean.User" alias="user"/>
       <typeAlias type="cn.tedu.store.bean.Goods" alias="goods"/>
    </typeAliases>
    -->
    <!-- 
	    为mapper文件中的参数类型,返回值类型定义别名
	    假如是采用package元素定义,
	    那默认类型的别名为类名并且
	    第一个单词的首字母小写.
     -->
    <typeAliases>
	  <package name="cn.tedu.store.bean"/>
    </typeAliases>
</configuration>
